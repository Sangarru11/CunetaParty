/* Fondo y márgenes generales */
.root {
    -fx-background-color: #f5f5f5; /* Color de fondo más suave */
}

.vbox {
    -fx-padding: 20;
    -fx-spacing: 20;
}

.hbox {
    -fx-spacing: 15;
}

/* Estilo para las imágenes de los iconos */
.image-view {
    -fx-cursor: hand;
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.2), 8, 0.1, 0, 0);
}

.image-view:hover {
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.4), 10, 0.1, 0, 0);
}

/* Estilo para los textos */
.text {
    -fx-font-family: 'Segoe UI', Tahoma, sans-serif;
    -fx-font-size: 18px;
    -fx-text-fill: #444444; /* Texto gris oscuro */
    -fx-cursor: hand;
}

/* Estilo para los títulos principales */
.text-title {
    -fx-font-size: 28px;
    -fx-font-weight: bold;
    -fx-text-fill: #333333;
    -fx-text-alignment: center;
    -fx-padding: 10px 0;
}

/* Estilo para los botones de texto e imágenes */
.hbox .text {
    -fx-margin-left: 10px;
}

/* Estilo de la tabla */
.table-view {
    -fx-background-color: #ffffff;
    -fx-border-color: #ddd;
    -fx-border-width: 1px;
    -fx-font-family: 'Segoe UI', Tahoma, sans-serif;
    -fx-font-size: 14px;
    -fx-selection-bar: #8fb2d4;
    -fx-selection-bar-non-focused: #f5f5f5;
}

.table-column {
    -fx-alignment: CENTER;
    -fx-font-size: 14px;
    -fx-text-fill: #444444;
}

.table-view .column-header {
    -fx-background-color: #8fb2d4; /* Fondo azul atractivo */
    -fx-text-fill: white;
}

.table-view .column-header:focused {
    -fx-background-color: #8fb2d4; /* Color más oscuro al estar enfocado */
}

.table-view .table-row-cell:hover {
    -fx-background-color: #f7f7f7; /* Hover con fondo claro */
}

/* Estilo para los botones de acción */
.button {
    -fx-background-color: #8fb2d4; /* Azul claro */
    -fx-text-fill: white;
    -fx-padding: 12px 20px;
    -fx-font-size: 16px;
    -fx-border-radius: 8px; /* Bordes redondeados */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.2), 5, 0.1, 0, 0);
}

.button:hover {
    -fx-background-color: #8fb2d4; /* Azul más oscuro al hacer hover */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.3), 6, 0.1, 0, 0);
}

/* Estilo para el área de contenido central */
.vbox.center {
    -fx-alignment: center;
}

.vbox.bottom-right {
    -fx-alignment: bottom-right;
}

/* Estilo para los márgenes y separación de elementos */
.hbox .text {
    -fx-margin-left: 10px;
    -fx-margin-right: 20px;
}

.vbox .text-title {
    -fx-padding: 0 0 10px 0;
}

